---

- name: Write macOS defaults
  osx_defaults:
    domain: "{{ item.domain }}"
    key: "{{ item.key }}"
    type: "{{ item.type }}"
    array_add: "{{ item.array_add | default(false) }}"
    value: "{{ item.value }}"
  with_items:
    - "{{ defaults }}"